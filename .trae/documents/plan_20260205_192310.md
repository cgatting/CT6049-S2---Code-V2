I will rebuild the application to be a fully local TypeScript full-stack application, removing the Java dependency.

### 1. Backend Reconstruction (Node.js + SQLite)
I will create a new `server` directory to replace the Java backend.
- **Dependencies**: `express` (server), `better-sqlite3` (database), `cors`, `bcryptjs` (security), `jsonwebtoken` (auth).
- **Database Schema**: I will recreate the schema (Operational + Warehouse tables) using raw SQL/SQLite to match the existing Java models.
- **Data Seeding**: I will port `DataSeeder.java` to TypeScript, ensuring User Roles match the frontend `types.ts` expectations (e.g., "Vice-chancellor" instead of "VICE_CHANCELLOR").
- **ETL Service**: I will port the logic from `ETLService.java` to a TypeScript service to handle the data transformation from Operational to Warehouse tables.

### 2. API Implementation
I will implement the following endpoints to match the current frontend calls:
- **Auth**: `POST /api/auth/login` (JWT based).
- **Dashboard**: `GET /api/dashboard/*` - I will implement the aggregation queries to provide real data instead of the current hardcoded frontend constants.
- **ETL**: `POST /api/etl/run` to trigger the ETL process manually if needed.

### 3. Frontend Integration
- **Proxy**: I will configure `vite.config.ts` to proxy `/api` requests to the local Node.js server (running on port 3001).
- **Dynamic Data**: I will update `Dashboard.tsx` to fetch data from the API instead of using hardcoded `FACULTY_STATS`, making the dashboard "live".
- **Login**: I will ensure `Login.tsx` communicates correctly with the new Node.js auth endpoint.

### 4. Cleanup & Execution
- I will remove the `backend/` (Java) directory.
- I will update `package.json` with a `dev` script that runs both the Vite frontend and the Node backend concurrently for a seamless "local app" experience.

**Note**: This will reset the database to a fresh state with seeded data.
